import 'package:flutter/material.dart';

class AppColors {
  // Primary palette
  static const Color primary = Color(0xFFFF6B8A);       // Blush pink
  static const Color primaryLight = Color(0xFFFFB3C4);   // Light pink
  static const Color primaryDark = Color(0xFFE84C6A);    // Deep rose

  // Secondary palette
  static const Color secondary = Color(0xFFC9B8FF);      // Soft lavender
  static const Color secondaryLight = Color(0xFFE8E1FF); // Light lavender
  static const Color secondaryDark = Color(0xFF9B87F5);  // Deep lavender

  // Accent
  static const Color accent = Color(0xFFFFB347);         // Warm peach
  static const Color accentLight = Color(0xFFFFD9A8);    // Light peach

  // Backgrounds
  static const Color background = Color(0xFFFFF8F9);     // Ivory warm white
  static const Color surface = Color(0xFFFFFFFF);        // Pure white
  static const Color surfaceVariant = Color(0xFFFDF0F3); // Blush tinted surface
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF2D1B2E);    // Deep plum
  static const Color textSecondary = Color(0xFF8E6B8E);  // Muted mauve
  static const Color textHint = Color(0xFFBDA8BD);       // Soft hint
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Utility
  static const Color divider = Color(0xFFF2E6F0);
  static const Color shadow = Color(0x18FF6B8A);
  static const Color overlay = Color(0x60000000);
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF66BB6A);
  static const Color online = Color(0xFF66BB6A);

  // Special
  static const Color heartRed = Color(0xFFFF4B6A);
  static const Color starGold = Color(0xFFFFD700);
  static const Color likeGreen = Color(0xFF4CD964);
  static const Color dislikeGray = Color(0xFF9E9E9E);
  static const Color superLikeBlue = Color(0xFF00B4D8);
  static const Color premiumGold = Color(0xFFFFAA00);
  static const Color premiumGoldLight = Color(0xFFFFF3CD);

  // Skeleton / shimmer
  static const Color shimmerBase = Color(0xFFEEDDEE);
  static const Color shimmerHighlight = Color(0xFFFAF0FA);

  // Transparent
  static const Color transparent = Colors.transparent;
}
